// APP
import { BaseControl, NovoControlConfig } from './../BaseControl';

export class EditorTemplateControl extends BaseControl {
    controlType = 'editor-template';

    constructor(config:NovoControlConfig) {
        super(config);
    }
}
